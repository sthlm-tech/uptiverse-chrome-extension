{
  "name": "test-extension",
  "private": true,
  "engines": {
    "node": ">=0.8.0"
  },
  "devDependencies": {
    "babel-core": "^6.7.2",
    "babel-preset-es2015": "^6.6.0",
    "babel-loader": "^6.2.7",
    "webpack": "^2.3.3"
  },
  "eslintConfig": {
    "env": {
      "node": true,
      "browser": true
    },
    "globals": {
      "chrome": true
    },
    "rules": {
      "eol-last": 0,
      "quotes": [
        2,
        "single"
      ]
    }
  },
  "dependencies": {
    "jwt-decode": "^2.2.0"
  },
  "scripts": {
    "build-dev": "webpack --watch --display-modules",
    "start-dev": "webpack-dev-server"
  }
}
